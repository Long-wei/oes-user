<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oes.mapper.UserMapper">
    <resultMap id="UserMap" type="com.oes.entity.po.User">
        <result property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="salt" column="salt" />
        <result property="email" column="email" />
        <result property="phone" column="phone" />
        <result property="realName" column="real_name" />
        <result property="avatar" column="avatar" />
        <result property="gender" column="gender" />
        <result property="status" column="status" />
        <result property="birthday" column="birthday" />
        <result property="lastLoginTime" column="last_login_time" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="lastLoginIp" column="last_login_ip" />
    </resultMap>


    <select id="selectLoginDtoByUsername" resultType="com.oes.entity.dto.LoginDto">
        select id, username, password, status, avatar, real_name from user where username = #{username}
    </select>

    <select id="selectUserById" resultMap="UserMap">
        select * from user where id = #{id}
    </select>

    <update id="updateUser" parameterType="com.oes.entity.po.User">
        update user
        <set>
            <if test="user.username != null">username = #{user.username},</if>
            <if test="user.password != null">password = #{user.password},</if>
            <if test="user.salt != null">salt = #{user.salt},</if>
            <if test="user.email != null">email = #{user.email},</if>
            <if test="user.phone != null"> phone = #{user.phone},</if>
            <if test="user.realName != null">real_name = #{user.realName},</if>
            <if test="user.avatar != null">avatar = #{user.avatar},</if>
            <if test="user.gender != null"> gender = #{user.gender},</if>
            <if test="user.birthday != null"> birthday = #{user.birthday},</if>
            <if test="user.status != null"> stauts = #{user.status},</if>
            <if test="user.lastLoginTime != null">last_login_time = #{user.lastLoginTime},</if>
            <if test="user.lastLoginIp != null">last_login_ip = #{user.lastLoginIp},</if>
            <if test="user.createTime != null">create_tiem = #{user.createTime},</if>
            <if test="user.updateTime != null">update_time = #{user.updateTime},</if>
        </set>
        where id = #{user.id}
    </update>

</mapper>